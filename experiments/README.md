# Experiment specifications

Executable experiment definitions are stored in `configs/`. Reproduction entry points are stored in `scripts/`. This directory is reserved for additional experiment manifests that do not contain generated results.
